{
  "read_file": {
    "prefix": "read_file",
    "body": [
      "with open(filename) as f:",
      "    content = f.readlines()",
      "# you may also want to remove whitespace characters like `\\n` at the end of each line",
      "content = [x.strip() for x in content]"
    ],
    "description": "read from file"
  },
  "json_dump_string": {
    "prefix": "json_dump_string",
    "body": ["json.dumps(object, ensure_ascii=False)"],
    "description": "dump python object to json string"
  },
  "json_dump_file": {
    "prefix": "json_dump_file",
    "body": ["json.dump(object, open(file, 'w'), ensure_ascii=False)"],
    "description": "dump python object to json file"
  },
  "json_load_string": {
    "prefix": "json_dump_string",
    "body": ["json.loads(json_string)"],
    "description": "dump python object to json string"
  }
}
